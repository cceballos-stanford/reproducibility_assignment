View(data)
colnames(data)
data <- read_excel("GroupA_6-1-2015/data/data_Exp1.xlsx", sheet = "summary")
head(data)
colnames(data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
tidy_data <- gather(data, key, value = value)
View(tidy_data)
# pivot_longer(data, cols = "closed-loop grasping")
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
# View(tidy_data)
pivot_longer(data)
?pivot_longer
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
# View(tidy_data)
pivot_longer(data, cols, names_to = "name")
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
# View(tidy_data)
pivot_longer(data, cols = "closed-loop grasping", names_to = "name")
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
pivot_longer(data, cols = "closed-loop grasping", names_to = "name")
View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
pivot_longer(data, cols = "closed-loop grasping", names_to = "sub")
View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
pivot_longer(data, cols = "closed-loop grasping", names_to = "sub", values_to = "trial")
View(tidy_data)
library(tidyverse)
majic_long <- majic %>%
pivot_longer(cols = "2015":"2016", names_to = "year", values_to = "score")
library(tidyverse)
mtcars
View(mtcars)
mtcars %>%
summarise(mean(mpg))
#Your code here. I did it completely differently w/o using the pipe function
View(mtcars$mpg)
mean(mtcars$mpg)
help(mpg)
mtcars %>%
group_by(cyl) %>%
summarise(mean(mpg))
#Your code here
mtcars %>%
group_by(cyl) %>%
summarise(mean(mpg), sd(mpg))
#Your code here
ggplot(mtcars, mapping = aes(x = hp, y = mpg)) + geom_point()
table3
help(table3)
View(table3)
table3_wide <- table3 %>%
spread(year, rate)
View(table3_wide)
#Your code here
table3_long <- table3_wide %>%
gather(year, rate, "1999", "2000")
View(table3_long)
#Your code here
table3_wide <- table3 %>%
pivot_wider(names_from = year, values_from = rate)
View(table3_wide)
table3_long <- table3_wide %>%
pivot_longer(cols = `1999`:`2000`, names_to = "year", values_to = "rate")
View(table3_long)
majic <- read_csv("data/majic.csv")
View(majic)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
pivot_longer(data, cols = "closed-loop"+"open-loop", names_to = "sub", values_to = "trial")
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
pivot_longer(data, cols = "closed-loop":"open-loop", names_to = "sub", values_to = "trial")
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
pivot_longer(data, cols = "closed-loop grasping", names_to = "sub", values_to = "trial")
View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
pivot_longer(data, cols = "closed-loop grasping", names_to = "sub1:sub10", values_to = "trial")
View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
pivot_longer(data, cols = "closed-loop grasping":"open-loop grasping":"crowded":"uncrowded", names_to = "sub1:sub10", values_to = "trial")
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
pivot_longer(data, cols = "sub1:sub10", names_to = "sub1:sub10", values_to = "trial")
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
pivot_longer(data, cols = "sub1", names_to = "sub1:sub10", values_to = "trial")
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
pivot_longer(data, cols = "sub", names_to = "sub1:sub10", values_to = "trial")
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
pivot_longer(data, cols = "open-loop grasping", names_to = "sub1:sub10", values_to = "trial")
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
pivot_longer(data, cols = "closed-loop grasping", names_to = "sub1:sub10", values_to = "trial")
View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
pivot_longer(data, cols = "crowded", names_to = "sub1:sub10", values_to = "trial")
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
pivot_longer(data, cols = "closed-loop grasping", names_to = "sub1:sub10", values_to = "trial")
View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
pivot_longer(data, cols = "open-loop_grasping", names_to = "sub1:sub10", values_to = "trial")
View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
pivot_longer(data, cols = "closed-loop grasping"&"open-loop_grasping", names_to = "sub1:sub10", values_to = "trial")
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
pivot_longer(data, cols = c(2:20), names_to = "sub1:sub10", values_to = "trial")
View(tidy_data)
data <- read_excel("GroupA_6-1-2015/data/data_Exp1.xlsx", sheet = "summary")
head(data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
colnames(data)
pivot_longer(data, cols = c(2:20), names_to = "sub1:sub10", values_to = "trial")
View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
colnames(data)
pivot_longer(data, cols = c(2:20), names_to = "sub1:sub10", values_to = "trial")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
colnames(data)
# pivot_longer(data, cols = c(2:20), names_to = "sub1:sub10", values_to = "trial")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
colnames(data)
pivot_longer(data, cols = c(1:20), names_to = c(1:20), values_to = "trial")
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
colnames(data)
pivot_longer(data, cols = c(1:20), names_to = c(1:10), values_to = "trial")
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
colnames(data)
pivot_longer(data, cols = c(1:20), names_to = "sub1", values_to = "trial")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
colnames(data)
pivot_longer(data, cols = c(1:20), names_to = "sub1", values_to = "trial")
View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
colnames(data)
# pivot_longer(data, cols = c(1:20), names_to = , values_to = "trial")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
colnames(data)
pivot_longer(data, cols = c(2:20), names_to = , values_to = "trial")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
colnames(data)
pivot_longer(data, cols = c(2:21), names_to = , values_to = "trial")
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
colnames(data)
pivot_longer(data, cols = c(2:20), names_to = , values_to = "trial")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
colnames(data)
pivot_longer(data, cols = c(2:15), names_to = , values_to = "trial")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
colnames(data)
pivot_longer(data, cols = c(2:15), names_to = , values_to = "trial")
View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
colnames(data)
# pivot_longer(data, cols = c(2:15), names_to = , values_to = "trial")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
colnames(data)
pivot_longer(data, cols = c(1:20), names_to = , values_to = "trial")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
colnames(data)
pivot_longer(data, cols = c(1:20), names_to = c(1:20), values_to = "trial")
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
colnames(data)
pivot_longer(data, cols = c(1:20), names_to = "name", values_to = "closed")
# View(tidy_data)
articleID <- "6-1-2015" # insert the article ID code here e.g., "10-3-2015"
reportType <- "Pilot" # specify whether this is the 'pilot' report or 'copilot' report
pilotNames <- "Cristina Ceballos" # insert the pilot's name here e.g., "Tom Hardwicke".
copilotNames <- "Insub Kim" # # insert the co-pilot's name here e.g., "Michael Frank".
pilotTTC <- 120 # insert the pilot's estimated time to complete (in minutes, it is fine to approximate) e.g., 120
copilotTTC <- NA # insert the co-pilot's estimated time to complete (in minutes, it is fine to approximate) e.g., 120
pilotStartDate <- "11/3/2019" # insert the piloting start date in US format e.g., as.Date("01/25/18", format = "%m/%d/%y")
copilotStartDate <- NA # insert the co-piloting start date in US format e.g., as.Date("01/25/18", format = "%m/%d/%y")
completionDate <- NA # insert the date of final report completion in US format e.g., as.Date("01/25/18", format = "%m/%d/%y")
# sets up some formatting options for the R Markdown document
knitr::opts_chunk$set(echo=TRUE, warning=FALSE, message=FALSE)
# load packages
library(tidyverse) # for data munging
library(knitr) # for kable table formating
library(haven) # import and export 'SPSS', 'Stata' and 'SAS' Files
library(readxl) # import excel files
library(ReproReports) # custom reporting functions
library(foreign)
# Prepare report object. This will be updated automatically by the reproCheck function each time values are compared
reportObject <- data.frame(dummyRow = TRUE, reportedValue = NA, obtainedValue = NA, valueType = NA, percentageError = NA, comparisonOutcome = NA, eyeballCheck = NA)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified"
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
colnames(data)
pivot_longer(data, cols = c(1:20), names_to = "name", values_to = "closed")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified"
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
head(data_m)
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
colnames(data)
pivot_longer(data, cols = c(1:20), names_to = "name", values_to = "closed")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified"
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
head(data_m)
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
# colnames(data)
# pivot_longer(data, cols = c(1:20), names_to = "name", values_to = "closed")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified"
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
head(data_m)
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
# colnames(data)
# pivot_longer(data, cols = c(1:20), names_to = "name", values_to = "closed")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified"
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
colname(data_m)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified"
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
col(data_m)
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
# colnames(data)
# pivot_longer(data, cols = c(1:20), names_to = "name", values_to = "closed")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified"
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
colnames(data_m)
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
# colnames(data)
# pivot_longer(data, cols = c(1:20), names_to = "name", values_to = "closed")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified". In this Excel file, I manually renamed each column. I also got rid of empty columns.
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
colnames(data_m)
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
# colnames(data)
# pivot_longer(data, cols = c(1:20), names_to = "name", values_to = "closed")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified". In this Excel file, I manually renamed each column. I also got rid of empty columns.
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
colnames(data_m)
# Now I use the functions "separate" and "pivot_longer" to make my data tidy.
separate(column, by = "_", into = c("open_closed","grasp_estim","crowded","cm"))
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified". In this Excel file, I manually renamed each column. I also got rid of empty columns.
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
colnames(data_m)
# Now I use the functions "separate" and "pivot_longer" to make my data tidy.
pivot_longer(data_m, closedloop_grasping_uncrowded_3cm:openloop_estimation_crowded_3.75cm, names_to = "column", values_to="estimate")
# separate(column, by = "_", into = c("open_closed","grasp_estim","crowded","cm"))
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
# colnames(data)
# pivot_longer(data, cols = c(1:20), names_to = "name", values_to = "closed")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified". In this Excel file, I manually renamed each column. I also got rid of empty columns.
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
colnames(data_m)
# Now I use the functions "separate" and "pivot_longer" to make my data tidy.
tidy_data <- pivot_longer(data_m, closedloop_grasping_uncrowded_3cm:openloop_estimation_crowded_3.75cm, names_to = "column", values_to="estimate")
# separate(column, by = "_", into = c("open_closed","grasp_estim","crowded","cm"))
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
# colnames(data)
# pivot_longer(data, cols = c(1:20), names_to = "name", values_to = "closed")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified". In this Excel file, I manually renamed each column. I also got rid of empty columns.
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
colnames(data_m)
# Now I use the functions "separate" and "pivot_longer" to make my data tidy.
tidy_data <- pivot_longer(data_m, closedloop_grasping_uncrowded_3cm:openloop_estimation_crowded_3.75cm, names_to = "column", values_to="estimate")
View(tidy_data)
# separate(column, by = "_", into = c("open_closed","grasp_estim","crowded","cm"))
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
# colnames(data)
# pivot_longer(data, cols = c(1:20), names_to = "name", values_to = "closed")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified". In this Excel file, I manually renamed each column. I also got rid of empty columns. I removed the "mean" row
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
colnames(data_m)
# Now I use the functions "separate" and "pivot_longer" to make my data tidy.
tidy_data <- pivot_longer(data_m, closedloop_grasping_uncrowded_3cm:openloop_estimation_crowded_3.75cm, names_to = "column", values_to="estimate")
View(tidy_data)
# separate(column, by = "_", into = c("open_closed","grasp_estim","crowded","cm"))
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
# colnames(data)
# pivot_longer(data, cols = c(1:20), names_to = "name", values_to = "closed")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified". In this Excel file, I manually renamed each column. I also got rid of empty columns. I removed the "mean" row
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
colnames(data_m)
# Now I use the functions "separate" and "pivot_longer" to make my data tidy.
tidy_data <- pivot_longer(data_m, closedloop_grasping_uncrowded_3cm:openloop_estimation_crowded_3.75cm, names_to = "column", values_to="estimate")
# View(tidy_data)
separate(tidy_data, column, by = "_", into = c("open_closed","grasp_estim","crowded","cm"))
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified". In this Excel file, I manually renamed each column. I also got rid of empty columns. I removed the "mean" row
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
colnames(data_m)
# Now I use the functions "separate" and "pivot_longer" to make my data tidy.
tidy_data <- pivot_longer(data_m, closedloop_grasping_uncrowded_3cm:openloop_estimation_crowded_3.75cm, names_to = "column", values_to="estimate")
# View(tidy_data)
separate(tidy_data, col, into = c("open_closed","grasp_estim","crowded","cm"), sep = "_")
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified". In this Excel file, I manually renamed each column. I also got rid of empty columns. I removed the "mean" row
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
colnames(data_m)
# Now I use the functions "separate" and "pivot_longer" to make my data tidy.
tidy_data <- pivot_longer(data_m, closedloop_grasping_uncrowded_3cm:openloop_estimation_crowded_3.75cm, names_to = "column", values_to="estimate")
# View(tidy_data)
separate(tidy_data, column, into = c("open_closed","grasp_estim","crowded","cm"), sep = "_")
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
# colnames(data)
# pivot_longer(data, cols = c(1:20), names_to = "name", values_to = "closed")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified". In this Excel file, I manually renamed each column. I also got rid of empty columns. I removed the "mean" row
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
colnames(data_m)
# Now I use the functions "separate" and "pivot_longer" to make my data tidy.
pivoted_data <- pivot_longer(data_m, closedloop_grasping_uncrowded_3cm:openloop_estimation_crowded_3.75cm, names_to = "column", values_to="estimate")
# View(tidy_data)
separated_data <- separate(pivoted_data, column, into = c("open_closed","grasp_estim","crowded","cm"), sep = "_")
View(separated_data)
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
# colnames(data)
# pivot_longer(data, cols = c(1:20), names_to = "name", values_to = "closed")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified". In this Excel file, I manually renamed each column. I also got rid of empty columns. I removed the "mean" row. I also removed the two top columsn.
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
colnames(data_m)
# Now I use the functions "separate" and "pivot_longer" to make my data tidy.
pivoted_data <- pivot_longer(data_m, closedloop_grasping_uncrowded_3cm:openloop_estimation_crowded_3.75cm, names_to = "column", values_to="estimate")
tidy_data <- separate(pivoted_data, column, into = c("open_closed","grasp_estim","crowded","cm"), sep = "_")
colnames(tidy_data)
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
# colnames(data)
# pivot_longer(data, cols = c(1:20), names_to = "name", values_to = "closed")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified". In this Excel file, I manually renamed each column. I also got rid of empty columns. I removed the "mean" row. I also removed the two top columsn.
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
# Now I use the functions "separate" and "pivot_longer" to make my data tidy.
pivoted_data <- pivot_longer(data_m, closedloop_grasping_uncrowded_3cm:openloop_estimation_crowded_3.75cm, names_to = "column", values_to="estimate")
tidy_data <- separate(pivoted_data, column, into = c("open_closed","grasp_estim","crowded","cm"), sep = "_")
colnames(tidy_data)
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
# colnames(data)
# pivot_longer(data, cols = c(1:20), names_to = "name", values_to = "closed")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified". In this Excel file, I manually renamed each column. I also got rid of empty columns. I removed the "mean" row. I also removed the two top columsn.
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
# Now I use the functions "separate" and "pivot_longer" to make my data tidy.
pivoted_data <- pivot_longer(data_m, closedloop_grasping_uncrowded_3cm:openloop_estimation_crowded_3.75cm, names_to = "column", values_to="estimate")
tidy_data <- separate(pivoted_data, column, into = c("open_closed","grasping_estimating","crowded_uncrowded","cm"), sep = "_")
colnames(tidy_data)
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
# colnames(data)
# pivot_longer(data, cols = c(1:20), names_to = "name", values_to = "closed")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified". In this Excel file, I manually renamed each column. I also got rid of empty columns. I removed the "mean" row. I also removed the two top columsn.
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
# Now I use the functions "separate" and "pivot_longer" to make my data tidy.
pivoted_data <- pivot_longer(data_m, closedloop_grasping_uncrowded_3cm:openloop_estimation_crowded_3.75cm, names_to = "column", values_to="estimate")
tidy_data <- separate(pivoted_data, column, into = c("open_closed","grasping_estimating","crowded_uncrowded","cm"), sep = "_")
View(tidy_data)
colnames(tidy_data)
# I want to create the following columns: grasping, ***
# tidy_data <- gather(data, key = "closed loop",  value = value)
# colnames(data)
# pivot_longer(data, cols = c(1:20), names_to = "name", values_to = "closed")
# View(tidy_data)
# Right now, the data is arranged in wide format. I want to make it longer, so I will use pivot_longer to make it tidy.
# But first, I have to manually modify the columns and assign each column a column name. I created a new Excel file called "data_Exp1_modified". In this Excel file, I manually renamed each column. I also got rid of empty columns. I removed the "mean" row. I also removed the two top columsn.
# I'll read in the new data.
data_m <- read_excel("GroupA_6-1-2015/data/data_Exp1_modified.xlsx", sheet = "summary")
# Now I use the functions "separate" and "pivot_longer" to make my data tidy.
pivoted_data <- pivot_longer(data_m, closedloop_grasping_uncrowded_3cm:openloop_estimation_crowded_3.75cm, names_to = "column", values_to="estimate")
tidy_data <- separate(pivoted_data, column, into = c("open_closed","grasping_estimating","crowded_uncrowded","cm"), sep = "_")
colnames(tidy_data)
# Now my data is tidy!
